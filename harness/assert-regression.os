#Использовать "./oscript"

Процедура Главная()
	Если АргументыКоманднойСтроки.Количество() < 1 Тогда
		Сообщить("usage: oscript assert-regression.os <report.json>");
		ЗавершитьРаботу(2);
	КонецЕсли;
	ЗавершитьРаботу(ПроверкаРегрессии.Проверить(АргументыКоманднойСтроки[0]));
КонецПроцедуры

Главная();
